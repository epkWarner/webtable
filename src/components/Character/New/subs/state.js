import React, { useReducer } from 'react'
import PropTypes from 'prop-types'

function NodeState(props) {
    return (
        <div>

        </div>
    )
}

NodeState.propTypes = {

}

export default useReducer(NodeState, {})